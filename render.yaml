services:
  - type: web
    name: vialifecoach-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    sourceDir: /
    envVars:
      - key: NODE_ENV
        value: production